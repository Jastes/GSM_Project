package GSM.Java_Project;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.Collections;

public class Minesweeper_Game {
    //why result there problem
//    private final Minesweeper_Setting mineSet;
//    private final ArrayList[][] mine;
//    ArrayList<String[]> mine = new ArrayList<String[]>();

    public Minesweeper_Game(){
//        mineSet = new Minesweeper_Setting();
//        String[] str_zero = new String[mineSet.getHeight()];
//        mine = new ArrayList[mineSet.getWidth()][mineSet.getHeight()];


//        for(int i=0; i < mineSet.getWidth(); i++)
//            mine.addAll(Collections.singleton(str_zero));

//        for(int i=0; i < mineSet.getWidth(); i++)
//            mine.add(new String[]{Arrays.toString(str_zero)});

    }

    /*public static void main(String[] args) {
        Minesweeper_Game game = new Minesweeper_Game();

        game.setMine();

        for (int i = 0; i < (game).mineSet.getWidth(); i++) {
            for (int j = 0; j < (game).mineSet.getHeight(); j++) {
                System.out.printf("%d ", game.mine[i][j].getState());
            }
            System.out.println();
        }
    }*/
}
